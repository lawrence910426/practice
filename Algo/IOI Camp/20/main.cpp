#include <iostream>
#include <random>
#include <time.h>
using namespace std;

int main () {
    srand(time(NULL));
    long long a ,b ,c ,d ,e;
    cout << 1 << endl;
    cout << rand() % 1000 + 3000 << endl;
    cout << rand() % 1000 + 3000 << endl;
    cout << rand() % 1000 + 3000 << endl;
    cout << rand() % 1000 + 3000 << endl;
    cout << rand() % 1000 + 3000 << endl;
}
